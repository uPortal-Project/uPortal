{
  "name": "uportal",
  "private": true,
  "devDependencies": {
    "eslint": "3.8.1",
    "eslint-config-google": "0.7.0",
    "stylelint": "7.7.1",
    "stylelint-config-standard": "15.0.1"
  },
  "scripts": {
    "test": "eslint . --ignore-path .gitignore && stylelint --syntax less **/*.less !**/bootstrap/**/*.less"
  },
  "eslintConfig": {
    "extends": "google"
  },
  "stylelint": {
    "extends": "stylelint-config-standard"
  }
}
